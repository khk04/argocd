 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
   name: backend
   namespace: monitor
 spec:
   storageClassName: rook-cephfs
   accessModes:
     - ReadWriteMany
   resources:
     requests:
       storage: 2Gi
