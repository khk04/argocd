apiVersion: v1
kind: Service
metadata:
  name: frontend-allso
  namespace: allso
spec:
  type: ClusterIP
  selector:
    app: frontend-allso
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    name: http