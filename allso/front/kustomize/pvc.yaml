 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
   name: frontend
   namespace: monitor
 spec:
   storageClassName: rook-cephfs
   accessModes:
     - ReadWriteMany
   resources:
     requests:
       storage: 2Gi
