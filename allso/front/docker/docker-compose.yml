#
# 이 파일은 도커를 관리하는 파일입니다. 
#
version: '2'
services:
    seegene-services-frontend-olaf-monitor-dev:
        build:
            context: ./development
            dockerfile: Dockerfile
        image: services/seegene-frontend-olaf-monitor-dev:latest

    seegene-services-frontend-olaf-monitor-build:
        build:
            context: ./development
            dockerfile: Dockerfile
        image: services/seegene-frontend-olaf-monitor-dev:latest
        volumes:
          - ${selected_directory}:/app
        ports:
          - 8082:8080
        working_dir: /app
        command: >
          bash -c "npm install --force && rm -rf /app/dist/ && npm run build"

    seegene-services-frontend-olaf-monitor:
      build:
        context: ./deployment
        dockerfile: Dockerfile
      image: services/seegene-frontend-olaf-monitor:latest

    seegene-services-frontend-olaf-monitor-init:
        build:
            context: ./development
            dockerfile: Dockerfile
        image: services/seegene-frontend-olaf-monitor-dev:latest
        user: "${USER_ID}:${GROUP_ID}"
        volumes:
          - ${selected_directory}:/app
        ports:
          - 8082:8080
        working_dir: /app
        command: >
          bash -c "vue create -d -f new" 
