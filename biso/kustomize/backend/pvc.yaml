 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
   name: backend
   namespace: wisdom
 spec:
   storageClassName: rook-cephfs
   accessModes:
     - ReadWriteMany
   resources:
     requests:
       storage: 2Gi
